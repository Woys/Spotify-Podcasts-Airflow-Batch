[pytest]
testpaths = tests
addopts = -p no:logfire --cov=include/spotify --cov=include/research --cov-report=term-missing --cov-fail-under=80
filterwarnings =
    ignore:ImportError while loading the `logfire-plugin` Pydantic plugin.*:UserWarning
